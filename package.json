{
  "name": "frontend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "npm run build --prefix frontend/ && set FLASK_ENV=development && set FLASK_APP=backend/main.py && flask run"
  }
}